# Basic scan — fails CI on HIGH+ findings
name: Bawbel Security Scan
on: [push, pull_request]

jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: bawbel/scan-action@v1
        with:
          path: .
          fail-on-severity: high
